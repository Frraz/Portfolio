# ========= DEPENDÊNCIAS DIRETAS =========
# Framework principal
fastapi==0.115.13

# Servidor ASGI em desenvolvimento e também útil em produção para websockets / hot reload
uvicorn[standard]==0.34.3
# O extra [standard] inclui: uvloop, httptools, websockets, watchfiles, etc.

# Engine WSGI para orquestrar múltiplos workers em produção (com UvicornWorker)
gunicorn==23.0.0

# Templates
Jinja2==3.1.6

# Variáveis de ambiente (.env)
python-dotenv==1.1.0

# Validação de email (se você realmente usa email_validator; caso contrário, remova)
email-validator==2.2.0

# Uploads multipart/form-data (necessário se futuramente aceitar formulário tradicional)
python-multipart==0.0.20

# ========= OPCIONAIS (descomente se precisar) =========
# Requests para chamadas HTTP externas (não está no seu código atual)
# requests==2.32.3

# Para enviar emails com DKIM, etc. (se sofisticar)
# aiosmtplib==3.0.2

# ========= AMBIENTE DE DESENVOLVIMENTO (opcional) =========
# black==24.10.0
# isort==5.13.2
# flake8==7.1.1
# mypy==1.11.2
# pytest==8.3.3
# httpx==0.27.2   # Para testes de endpoints async